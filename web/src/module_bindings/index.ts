/**
 * Placeholder module bindings.
 *
 * Replace by running:
 *   spacetime generate --lang typescript --out-dir web/src/module_bindings --project-path server
 *
 * Until then, connectToSpacetime reports a clear TODO in the HUD.
 */

export const BINDINGS_GENERATED = false;

/** Stub so dynamic import succeeds before generate. */
export const DbConnection = undefined;
